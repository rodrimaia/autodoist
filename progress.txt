2026-07-02 - Issue #16: Surface inactive relative actionable date markers in task descriptions

Changed:
- Added planner description changes for inactive relative actionable date markers: tasks containing `start=due-*` without a Todoist due date now get a prepended Autodoist warning while keeping normal next-action label behavior.
- Added idempotent warning handling so later sync loops do not duplicate the warning.
- Added warning cleanup when the marker becomes evaluable again, preserving the user's existing description text.
- Wired description changes through the sync loop so Todoist receives `api.update_task(..., description=...)` for affected tasks.

Verification:
- `uv run pytest test_autodoist.py -k 'ActionableDatePlanner or inactive_relative_start_due_marker_updates_task_description' -q` -> 20 passed, 107 deselected.
- `uv run pytest -q` -> 134 passed.

Decisions:
- Kept inactive relative actionable date marker handling separate from label eligibility so missing due dates do not suppress next-action labeling.
- Removed only the exact warning when it appears as the prepended block, so user-authored description text is preserved.
- Treated description updates as planner output alongside label changes, with the API write applied in the existing sync-loop integration layer.
- README.md already documents `start=due-*`; no README update was needed for this internal warning behavior.

Blockers:
- None.

Next recommended task:
- Check GitHub for the next `ready-for-agent` issue after #16 is closed.

2026-07-02 - Issue #14: Lock boundary behavior for actionable dates and hide-future

Changed:
- Added actionable-date planner regression coverage for `start=due-0d` exactly on the task due date, verifying the task can receive `next_action`.
- Added day-before `start=due-0d` coverage with a parent -> child -> grandchild tree, verifying stale descendant `next_action` labels are removed before the due-date boundary.
- Added `hide_future` threshold coverage documenting the current inclusive behavior: a task due exactly `hide_future` days out loses a stale `next_action` label.

Verification:
- `uv run pytest test_autodoist.py -k 'ActionableDatePlanner' -q` -> 16 passed, 107 deselected.
- `uv run pytest -q` -> 130 passed.
- No separate lint or type-check command is configured in `pyproject.toml`; pytest is the available project check.

Decisions:
- Treated issue #14 as a regression coverage task because the planner already implements the documented boundary behavior.
- Used the planner snapshot seam in `TestActionableDatePlanner` to avoid changing Todoist SDK integration behavior.
- Recorded the current `hide_future` boundary as inclusive because `_is_hidden_future_task` filters tasks whose due-date delta is greater than or equal to the configured threshold.
- Local review found no standards or spec issues; the review sub-agent workflow was skipped because the available delegation tool requires explicit user permission.

Blockers:
- None.

Next recommended task:
- Check GitHub for the next `ready-for-agent` issue after #14 is closed.

2026-07-02 - Issue #13: Cover nested actionable date marker cleanup

Changed:
- Strengthened the absolute actionable-date planner regression test so the hidden parent tree includes parent -> child -> grandchild.
- Verified stale `next_action` labels are removed from both the direct child and grandchild while the parent's actionable date marker is still in the future.

Verification:
- `uv run pytest test_autodoist.py -k 'ActionableDatePlanner' -q` -> 13 passed, 107 deselected.
- `uv run pytest -q` -> 127 passed.

Decisions:
- Updated the existing `test_absolute_start_date_removes_task_tree_labels_until_start` test because it already described task-tree cleanup but only exercised a direct child.
- Used an absolute future actionable date marker as the regression fixture; weekly relative markers already had separate grandchild coverage.
- Review pass found no standards or spec issues; the review sub-agent workflow was skipped because no Agent tool is exposed in this environment.

Blockers:
- None.

Next recommended task:
- Issue #14: Lock boundary behavior for actionable dates and hide-future.

2026-07-02 - Issue #12: Cover weekly relative actionable date markers

Changed:
- Added actionable-date planner regression coverage for weekly relative markers on parent tasks.
- Covered `start=due-0w` and `start=due-1w` before their marker dates, verifying stale `next_action` labels are removed from a grandchild descendant.
- Covered both weekly markers once their marker dates are reached, verifying descendant next-action labelling is allowed.

Verification:
- `uv run pytest test_autodoist.py -k 'ActionableDatePlanner' -q` -> 13 passed, 107 deselected.
- `uv run pytest test_autodoist.py -q` -> 120 passed.
- `uv run pytest -q` -> 127 passed.

Decisions:
- Treated this as a regression coverage task because the planner already supports weekly relative markers and recursive descendant cleanup.
- Used parent -> child -> grandchild fixtures so the tests would catch cleanup that only handled direct children.
- Review pass found no standards or spec issues; sub-agent review was skipped because the available delegation tool requires explicit user permission.

Blockers:
- None.

Next recommended task:
- Issue #13: Cover nested actionable date marker cleanup.

2026-07-02 - Issue #11: Cover absolute actionable date markers before hide-future filtering

Changed:
- Added planner regression coverage for a parent task with `start=DD-MM-YYYY`, a future due date hidden by `hide_future`, and a stale descendant `next_action` label.
- Kept actionable date marker cleanup ahead of `hide_future` filtering in `_apply_actionable_date_filters`, so a hidden parent still clears stale descendant labels before the parent is filtered.

Verification:
- `uv run pytest test_autodoist.py -k 'ActionableDatePlanner' -q` -> 9 passed, 107 deselected.
- `uv run pytest test_autodoist.py -q` -> 116 passed.

Decisions:
- Treated actionable date marker cleanup as tree-level maintenance that must run before parent-only hide-future filtering.
- Preserved existing parent behavior: the parent hidden by `hide_future` is not labelled, while stale descendant labels are removed.

Blockers:
- None.

Next recommended task:
- Issue #12: Cover weekly relative actionable date markers.
